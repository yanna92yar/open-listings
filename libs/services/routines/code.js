/**
 * absorb lowers TypeScript feaver
 * @param {any} variable
 */
export function absorb(variable) {
    typeof variable
}

export const to = (promise) => promise.then((data) => [null, data]).catch((err) => [err, null])
